apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tpm-tools-backend.fullname" . }}-test-connection"
  labels:
    {{- include "tpm-tools-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tpm-tools-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
